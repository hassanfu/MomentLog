# ── DeepSeek（AI 简报：OpenAI 兼容接口，国内可用）─────────────
# https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=sk-...
# 部署到 Vercel：在网页里加同名变量，或本机已 `npx vercel link` 后运行
# npm run vercel:push-deepseek （把本地 .env.local 同步到 Production/Preview/Development）
